<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ExpressionSearchMapper">
    <select id="searchExpression" resultType="map">
        SELECT gene_id, ${columns}
        FROM ${tableName}
        WHERE gene_id IN
        <foreach item="id" collection="targetIds" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="getSampleColumns" resultType="string">
        SELECT column_name
        FROM information_schema.columns
        WHERE table_name = #{tableName}
          AND column_name != 'gene_id'
    </select>
    <select id="searchAllExpression" resultType="java.util.Map">
        SELECT gene_id, ${selectedSamples}
        FROM ${tableName}
        ORDER BY gene_id ASC
    </select>
</mapper>